{
  "name": "agent-discoveries-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "start": "ts-node-dev --respawn --transpile-only -r dotenv/config ./src/server.ts",
    "start:prod": "npm run build && node ./build/server.js",
    "test": "jest"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.1",
    "express-session": "^1.17.3"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "@types/express": "^4.17.14",
    "@types/jest": "^29.1.1",
    "@types/supertest": "^2.0.12",
    "jest": "^29.1.2",
    "supertest": "^6.3.0",
    "ts-jest": "^29.0.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.4"
  }
}
